# ⚡ คู่มือการตั้งค่าแบบเร็ว

## 🚨 ปัญหาที่พบบ่อย: "กรุณาตั้งค่า URL ของเว็บแอปใน Extension"

### วิธีแก้ไข:

#### 1. ตั้งค่า Extension
1. **คลิกไอคอน Extension** ในแถบเครื่องมือ Chrome
2. **ใส่ TMDB API Key:**
   - ไปที่ https://www.themoviedb.org/settings/api
   - สร้าง API Key ใหม่
   - คัดลอกและใส่ในช่อง "TMDB API Key"
3. **ใส่ Web App URL:**
   - ใส่ `http://127.0.0.1:5000` (ถ้าเว็บแอปทำงานในเครื่อง)
   - หรือ URL ของเซิร์ฟเวอร์ที่ deploy

#### 2. ตรวจสอบเว็บแอป
1. **เปิดเว็บแอป Movie Info App:**
   ```bash
   python app.py
   ```
2. **ตรวจสอบว่าเว็บแอปทำงาน:**
   - เปิด http://127.0.0.1:5000 ในเบราว์เซอร์
   - ควรเห็นหน้าเว็บแอปปรากฏ

#### 3. ทดสอบการเชื่อมต่อ
1. **เปิดหน้าหนังใน TMDB:**
   - ไปที่ https://www.themoviedb.org/movie/1022787
2. **คลิกปุ่มลอย "+ เพิ่มหนัง"**
3. **ตรวจสอบ notification:**
   - ✅ สำเร็จ: "เพิ่มหนังสำเร็จ"
   - ❌ ผิดพลาด: ดูข้อความแจ้งเตือน

## 🔧 การแก้ไขปัญหาเพิ่มเติม

### ถ้ายังไม่ทำงาน:
1. **รีเฟรช Extension:**
   - ไปที่ `chrome://extensions/`
   - คลิก **Reload** ที่ Extension

2. **ตรวจสอบ Console:**
   - กด F12 เปิด Developer Tools
   - ดู Console สำหรับข้อผิดพลาด

3. **ตรวจสอบ Network:**
   - ดู Network tab ใน Developer Tools
   - ตรวจสอบการเรียก API

### ข้อความแจ้งเตือนที่พบบ่อย:
- **"กรุณาตั้งค่า URL ของเว็บแอป"** → ใส่ URL ใน Extension
- **"กรุณาตั้งค่า TMDB API Key"** → ใส่ API Key ใน Extension
- **"ไม่สามารถเชื่อมต่อกับเว็บแอปได้"** → ตรวจสอบว่าเว็บแอปทำงานอยู่

## ✅ หลังจากตั้งค่าเสร็จ

Extension จะสามารถ:
- คลิกปุ่มลอย "+ เพิ่มหนัง" บนหน้า TMDB
- นำเข้าข้อมูลหนังไปยัง Movie Info App
- แสดง notification แจ้งผลการทำงาน

**สนุกกับการใช้งาน! 🎬✨**
